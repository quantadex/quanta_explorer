import { connect } from 'react-redux';
import DeployCrossChain from '@quanta/components/pages/DeployCrossChain';

const mapStateToProps = state => ({});

const mapActionCreators = dispatch => ({});

export default connect(
	mapStateToProps,
	mapActionCreators
)(DeployCrossChain);
